# Copy to .env (already gitignored) and fill in real values.

# Only needed if you switch database.js off the local-SQLite-in-volume setup
# (see docker-compose.yml) and back to Turso.
# TURSO_AUTH_TOKEN=

# SpendOS connector (read-only/advisory integration)
SPENDOS_TENANT_ID=total-tools
# SPENDOS_INGEST_URL=https://spendos.example.com/v1/events
# SPENDOS_API_KEY=
SPENDOS_OUTBOX_BATCH=25
