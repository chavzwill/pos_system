(() => {
  'use strict';
  const API = '/api/technician-compensation';
  const state = { allowed:false, open:false, summary:null, date:new Date().toISOString().slice(0,10), working:false, editor:null };
  const money = n => new Intl.NumberFormat('en-JM', { style:'currency', currency:'JMD', maximumFractionDigits:2 }).format(Number(n)||0);
  const esc = s => String(s ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  async function request(path, init={}) { const r=await fetch(API+path,{credentials:'same-origin',headers:{Accept:'application/json',...(init.body?{'Content-Type':'application/json'}:{}),...(init.headers||{})},...init}); const p=await r.json().catch(()=>({})); if(!r.ok) throw new Error(p.error||'Technician compensation is unavailable'); return p; }
  async function probe(){ try{ await request('/period'); state.allowed=true; install(); } catch(e){ if(!/Authentication|required|permission/i.test(String(e.message))) console.warn('Technician pay probe:',e.message); } }
  function install(){ if(document.getElementById('tt-tech-pay-launcher'))return; const b=document.createElement('button'); b.id='tt-tech-pay-launcher'; b.className='tt-tech-pay-launcher'; b.type='button'; b.dataset.guideId='technician-compensation'; b.textContent='Technician Pay'; b.addEventListener('click',open); document.body.appendChild(b); }
  async function load(){ state.working=true; render(); try{ state.summary=await request('/summary?date='+encodeURIComponent(state.date)); } catch(e){ state.summary={error:e.message,rows:[]}; } finally { state.working=false; render(); } }
  function open(){ state.open=true; const existing=document.getElementById('tt-tech-pay-root'); if(existing) existing.remove(); const root=document.createElement('div'); root.id='tt-tech-pay-root'; document.body.appendChild(root); void load(); }
  function close(){ document.getElementById('tt-tech-pay-root')?.remove(); state.open=false; state.editor=null; document.getElementById('tt-tech-pay-launcher')?.focus(); }
  function evidence(row){ const labels={time_entries:'Time',task_completion:'Tasks',attendance:'Attendance',overtime_hours:'OT',qc_first_pass:'QC',comeback_rework:'Rework',safety_events:'Safety'}; return Object.entries(row.evidence||{}).map(([k,v])=>'<span class="'+(v==='unavailable'?'is-missing':'')+'" title="'+esc(v)+'">'+esc(labels[k]||k)+': '+esc(v==='unavailable'?'Unavailable':'Verified')+'</span>').join(''); }
  function rowMarkup(r){ const efficiency=r.efficiency_percent==null?'Efficiency unavailable':Number(r.efficiency_percent).toFixed(1)+'% allotted/actual'; return '<tr><td><strong>'+esc(r.first_name)+' '+esc(r.last_name)+'</strong><small>'+esc(r.employee_number||'')+'</small></td><td><strong>'+Number(r.worked_hours||0).toFixed(2)+' h</strong><small>'+Number(r.worked_minutes||0).toFixed(1)+' min</small></td><td><strong>'+esc(r.completed_tasks)+' completed</strong><small>'+esc(efficiency)+'</small></td><td class="tt-tech-pay__money">'+money(r.hourly_rate)+'</td><td class="tt-tech-pay__money">'+money(r.base_pay)+'</td><td class="tt-tech-pay__money">'+money(r.adjustments_total)+'</td><td class="tt-tech-pay__money">'+money(r.payable_total)+'</td><td><div class="tt-tech-pay__evidence">'+evidence(r)+'</div><div class="tt-tech-pay__actions"><button class="tt-action" type="button" data-rate="'+Number(r.employee_id)+'">Set rate</button><button class="tt-action" type="button" data-adjust="'+Number(r.employee_id)+'">Adjustment</button><button class="tt-action is-primary" type="button" data-finalize="'+Number(r.employee_id)+'">Finalize</button></div></td></tr>'; }
  function render(){ const root=document.getElementById('tt-tech-pay-root'); if(!root)return; const s=state.summary; const rows=s?.rows||[]; let period='Select a date to load the applicable period.'; if(s?.period) period='<strong>'+esc(s.period.start)+'</strong> → <strong>'+esc(s.period.end)+'</strong> · '+esc(s.period.label); let notice=''; if(state.working) notice='<div class="tt-tech-pay__notice">Loading verified technician evidence…</div>'; else if(s?.error) notice='<div class="tt-tech-pay__notice is-error">'+esc(s.error)+'</div>'; else if(s?.note) notice='<div class="tt-tech-pay__notice">'+esc(s.note)+'</div>'; let table=''; if(!state.working&&!s?.error) table=rows.length?'<div class="tt-tech-pay__table-wrap"><table><thead><tr><th>Technician</th><th>Verified time</th><th>Tasks / efficiency</th><th>Rate</th><th>Base pay</th><th>Adjustments</th><th>Payable</th><th>Evidence / controls</th></tr></thead><tbody>'+rows.map(rowMarkup).join('')+'</tbody></table></div>':'<div class="tt-tech-pay__empty">No completed technician timer evidence exists for this pay period.</div>'; root.innerHTML='<div class="tt-tech-pay-overlay" data-pay-close><section class="tt-tech-pay" role="dialog" aria-modal="true" aria-labelledby="tt-tech-pay-title" onclick="event.stopPropagation()"><header class="tt-tech-pay__head"><div><span>Total Tools Repairs</span><h2 id="tt-tech-pay-title">Technician Compensation</h2><p>Verified work evidence and controlled pay-period totals.</p></div><button type="button" aria-label="Close technician compensation" data-pay-close>×</button></header><div class="tt-tech-pay__body"><div class="tt-tech-pay__toolbar"><label>Pay-period date<input type="date" id="tt-pay-date" value="'+esc(state.date)+'"></label><div class="tt-tech-pay__period">'+period+'</div></div>'+notice+table+'<div id="tt-tech-pay-form" class="tt-tech-pay__form"></div></div></section></div>'; bind(); }
  function bind(){ const root=document.getElementById('tt-tech-pay-root'); if(!root)return; root.querySelectorAll('[data-pay-close]').forEach(x=>x.addEventListener('click',close)); root.querySelector('#tt-pay-date')?.addEventListener('change',e=>{state.date=e.target.value;void load();}); root.querySelectorAll('[data-rate]').forEach(b=>b.addEventListener('click',()=>showRate(Number(b.dataset.rate)))); root.querySelectorAll('[data-adjust]').forEach(b=>b.addEventListener('click',()=>showAdjustment(Number(b.dataset.adjust)))); root.querySelectorAll('[data-finalize]').forEach(b=>b.addEventListener('click',()=>void finalize(Number(b.dataset.finalize),b))); }
  function targetRow(id){ return state.summary?.rows?.find(r=>Number(r.employee_id)===Number(id)); }
  function showRate(id){ const row=targetRow(id),f=document.getElementById('tt-tech-pay-form'); if(!f||!row)return; f.classList.add('is-open'); f.innerHTML='<h3>Set technician rate · '+esc(row.first_name)+' '+esc(row.last_name)+'</h3><form id="tt-rate-form"><div class="tt-tech-pay__form-grid"><label>Hourly rate<input name="hourly_rate" type="number" min="0" step="0.01" value="'+(Number(row.hourly_rate)||0)+'" required></label><label>Overtime rate<input name="overtime_rate" type="number" min="0" step="0.01" placeholder="Stored for future verified OT evidence"></label><label>Effective from<input name="effective_from" type="date" value="'+esc(state.summary.period.start)+'" required></label></div><label>Change note<textarea name="change_note" placeholder="Why is this rate changing?"></textarea></label><footer><button class="tt-action" type="button" data-form-cancel>Cancel</button><button class="tt-action is-primary" type="submit">Save rate</button></footer></form>'; f.querySelector('[data-form-cancel]').onclick=()=>{f.classList.remove('is-open');f.innerHTML='';}; f.querySelector('form').onsubmit=async e=>{e.preventDefault();const d=new FormData(e.currentTarget);try{await request('/rates/'+id,{method:'PUT',body:JSON.stringify(Object.fromEntries(d.entries()))});f.classList.remove('is-open');await load();}catch(err){alert(err.message);}}; f.scrollIntoView({behavior:'smooth',block:'center'}); }
  function showAdjustment(id){ const row=targetRow(id),f=document.getElementById('tt-tech-pay-form'); if(!f||!row)return; f.classList.add('is-open'); f.innerHTML='<h3>Pay adjustment · '+esc(row.first_name)+' '+esc(row.last_name)+'</h3><form id="tt-adjust-form"><div class="tt-tech-pay__form-grid"><label>Type<select name="adjustment_type"><option value="incentive">Incentive</option><option value="correction">Correction</option><option value="deduction">Deduction</option><option value="other">Other</option></select></label><label>Amount<input name="amount" type="number" step="0.01" required></label></div><label>Audit note<textarea name="note" required placeholder="State the evidence/reason for this adjustment."></textarea></label><footer><button class="tt-action" type="button" data-form-cancel>Cancel</button><button class="tt-action is-primary" type="submit">Record adjustment</button></footer></form>'; f.querySelector('[data-form-cancel]').onclick=()=>{f.classList.remove('is-open');f.innerHTML='';}; f.querySelector('form').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget).entries());d.employee_id=id;d.period_start=state.summary.period.start;d.period_end=state.summary.period.end;try{await request('/adjustments',{method:'POST',body:JSON.stringify(d)});f.classList.remove('is-open');await load();}catch(err){alert(err.message);}}; f.scrollIntoView({behavior:'smooth',block:'center'}); }
  async function finalize(id,button){ const row=targetRow(id); if(!row)return; if(!confirm('Finalize '+row.first_name+' '+row.last_name+' for '+state.summary.period.start+' to '+state.summary.period.end+'? This freezes the current verified evidence snapshot.'))return; button.disabled=true; try{await request('/finalize/'+id,{method:'POST',body:JSON.stringify({period_start:state.summary.period.start,period_end:state.summary.period.end})});alert('Technician pay-period snapshot finalized.');await load();}catch(e){alert(e.message);}finally{button.disabled=false;} }
  document.addEventListener('keydown',e=>{if(e.key==='Escape'&&state.open)close();});
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',()=>setTimeout(probe,250)); else setTimeout(probe,250);
})();
