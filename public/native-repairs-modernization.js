(()=>{'use strict';
const ROOT_ID='tt-work-orders-workspace';
function qs(sel,root=document){return root.querySelector(sel)}
function qsa(sel,root=document){return [...root.querySelectorAll(sel)]}
function visibleRoot(){return document.getElementById(ROOT_ID)}
function counts(root){const rows=qsa('.tt-wo-row',root);const total=rows.length;const active=rows.filter(r=>r.querySelector('.tt-wo-pill.is-in_progress')).length;const signoff=rows.filter(r=>r.querySelector('.tt-wo-pill.is-awaiting_signoff')).length;const pickup=rows.filter(r=>r.querySelector('.tt-wo-pill.is-awaiting_pickup')).length;return{total,active,signoff,pickup}}
function installBar(root){if(qs('.tt-repair-modern-bar',root))return;const top=qs('.tt-wo__top',root);if(!top)return;const bar=document.createElement('div');bar.className='tt-repair-modern-bar';bar.innerHTML='<strong>Repair command</strong><button data-repair-command="search">Find work order</button><button data-repair-command="refresh">Refresh queue</button><button data-repair-command="history">Equipment history</button><button data-repair-command="guide">Guided Mode</button><span>/ search · R refresh · Esc back</span>';top.insertAdjacentElement('afterend',bar);bar.addEventListener('click',e=>{const b=e.target.closest('[data-repair-command]');if(!b)return;const cmd=b.dataset.repairCommand;if(cmd==='search')qs('#tt-wo-search',root)?.focus();if(cmd==='refresh')window.TotalToolsWorkOrdersWorkspace?.refresh?.();if(cmd==='history')qs('[data-module="repair-operations"]',root)?.click();if(cmd==='guide')window.TotalToolsGuidedMode?.open?.('repair')||window.TotalToolsNativePosShell?.openCommandPalette?.('repair');});}
function installKpis(root){const pane=qs('.tt-wo__list-pane',root);if(!pane)return;let strip=qs('.tt-repair-kpi-strip',root);if(!strip){strip=document.createElement('div');strip.className='tt-repair-kpi-strip';const filters=qs('.tt-wo__filters',pane);filters?.insertAdjacentElement('afterend',strip)}const c=counts(root);strip.innerHTML=`<div><small>Queue</small><strong>${c.total}</strong></div><div><small>In progress</small><strong>${c.active}</strong></div><div><small>Awaiting signoff</small><strong>${c.signoff}</strong></div><div><small>Awaiting pickup</small><strong>${c.pickup}</strong></div>`;}
function enhance(){const root=visibleRoot();if(!root)return;installBar(root);installKpis(root);}
const observer=new MutationObserver(()=>enhance());observer.observe(document.documentElement,{subtree:true,childList:true});
document.addEventListener('keydown',e=>{const root=visibleRoot();if(!root)return;if(e.key==='/'&&!/input|textarea|select/i.test(document.activeElement?.tagName||'')){e.preventDefault();qs('#tt-wo-search',root)?.focus()}if((e.key==='r'||e.key==='R')&&!e.ctrlKey&&!e.metaKey&&!/input|textarea|select/i.test(document.activeElement?.tagName||'')){e.preventDefault();window.TotalToolsWorkOrdersWorkspace?.refresh?.()}if(e.key==='Escape'&&!document.querySelector('.tt-sales,.pos-command-overlay')){qs('[data-close]',root)?.click()}});
window.TotalToolsNativeRepairs={enhance};
enhance();
})();